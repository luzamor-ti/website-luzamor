# GitHub Actions CI Environment Variables
# These are fallback values used when secrets are not configured

# Sanity Configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=test-project-id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2026-02-11
